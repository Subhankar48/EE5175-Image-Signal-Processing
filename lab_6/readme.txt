The Jupyter notebook already has the generated results. 

It assumes that the image files are present in the same directory
as the notebook.  

All the results can be regenerated by running all the cells again.
